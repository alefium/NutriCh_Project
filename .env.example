# NutriCh_Project .env template
# Copy this file to .env and fill required values. Do not commit the .env file.

# Required - used by Flask for session security
FLASK_SECRET_KEY=change-this-to-a-long-random-string

# OpenRouter key to enable LLM features via LiteLLM, it is necessary to have the LLM working but without is there is a fallback mechanist anyway
OPENROUTER_API_KEY=put_here_your_openrouter_apy_key

# Optional - LLM model to use when OpenRouter is configured, it is already present in the code anyway
LLM_MODEL=openrouter/meta-llama/llama-3.3-70b-instruct

# Optional - App display name sent to OpenRouter
APP_NAME=NutRich-Chatbot

# Optional - HTTP Referer sent to OpenRouter for usage tracking
OPENROUTER_REFERER=https://yourdomain.com

# Optional - Similarity threshold for answer selection (0 to 1), it is already present in the code anyway
SIMILARITY_THRESHOLD=0.75
