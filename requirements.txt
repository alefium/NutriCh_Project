# Python version python_version==3.13.5 (this is the version used to build the project, probably older versions works anyway)

# Flask web framework for the web application
Flask==3.1.1

# ChromaDB for vector database functionality
chromadb==1.0.16

# Sentence transformers for generating text embeddings
sentence-transformers==5.1.0

# Werkzeug for secure file uploads (included with Flask but listing for clarity)
Werkzeug==3.1.1

# LiteLLM for LLM integration with OpenRouter
litellm==1.75.0

# Python-dotenv for environment variable management
python-dotenv==1.1.1

# numpy used in the app
numpy==2.3.2

# openai:
installed openai==1.99.9